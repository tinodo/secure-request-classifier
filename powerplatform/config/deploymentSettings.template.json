{
  "$schema": "https://raw.githubusercontent.com/microsoft/powerplatform-actions/main/schemas/deploymentSettings.schema.json",

  "//": [
    "Deployment settings for `pac solution import --settings-file`.",
    "",
    "Placeholder tokens are an upper-case name wrapped in #{ and }# and are replaced at",
    "deployment time by scripts/New-DeploymentSettings.ps1 using GitHub repository SECRETS",
    "(and a few non-sensitive VARIABLES) plus outputs from the Bicep deployment. Commit this",
    "template; never commit a rendered copy.",
    "",
    "Do not write a literal placeholder token anywhere in these comments. The renderer scans",
    "the whole file, so an example token here is treated as a real one that must be supplied,",
    "and rendering fails with 'No value supplied for required token(s)'.",
    "",
    "THERE IS DELIBERATELY NO ConnectionReferences SECTION.",
    "",
    "Both connectors use delegated-user OAuth, so a person creates the connections once, in the",
    "flow designer, and binds them there. The pipeline must not rebind them: the import runs as",
    "the deployment service principal, which has no permission on a connection owned by a user,",
    "so binding fails with ConnectionAuthorizationFailed and destroys the person's work in the",
    "process. Leaving the section out means every later import preserves whatever the human",
    "bound. See docs/limitations.md."
  ],

  "EnvironmentVariables": [
    {
      "SchemaName": "srcls_FunctionBaseUrl",
      "Value": "#{FUNCTION_BASE_URL}#"
    },
    {
      "SchemaName": "srcls_FunctionClassifyPath",
      "Value": "/api/requests/classify"
    },
    {
      "SchemaName": "srcls_FunctionApplicationIdUri",
      "Value": "#{FUNCTION_APPLICATION_ID_URI}#"
    },
    {
      "SchemaName": "srcls_EnvironmentLabel",
      "Value": "#{ENVIRONMENT_LABEL}#"
    }
  ]
}
